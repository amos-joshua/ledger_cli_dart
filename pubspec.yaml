name: ledger_cli
description: A starting point for Dart libraries or applications.
version: 0.1.0
publish_to: 'none'
homepage: https://www.example.com

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
#  path: ^1.8.0
  csv: ^5.0.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
